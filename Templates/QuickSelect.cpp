#include<bits/stdc++.h>
using namespace std;
typedef long long ll;

ll QuickSelect(vector<ll> v, ll k){
    if(v.size()==1) return v[0];
    ll x = v[0];
}

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    
}
